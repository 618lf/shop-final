<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tmt.system.dao.UpdateDataDao" >
  <resultMap id="BaseResult" type="com.tmt.system.entity.UpdateData"> 
    <result column="ID" property="id" jdbcType="BIGINT"/>
    <result column="MSG" property="msg" jdbcType="VARCHAR"/>
    <result column="MODULE" property="module" jdbcType="TINYINT"/>
    <result column="OPT" property="opt" jdbcType="TINYINT"/>
  </resultMap>
  
  <select id="findByCondition" resultMap="BaseResult">
   SELECT ID, MSG, MODULE, OPT FROM SYS_UPDATE_DATA WHERE STATE = 0
  </select>
  
  <select id="queryByModule" resultMap="BaseResult" parameterType="java.util.Map">
   SELECT ID, MSG, MODULE, OPT FROM SYS_UPDATE_DATA WHERE STATE = 0 AND MODULE IN(${MODULES})
  </select>
  
  <delete id="delete" parameterType="java.util.List">
   DELETE FROM SYS_UPDATE_DATA WHERE ID IN
   <foreach collection="list" item="item" index="index" separator=","  open="(" close=")">  
    #{item.id}
   </foreach> AND STATE = 1
  </delete>
  
  <insert id="insert" parameterType="com.tmt.system.entity.UpdateData">
   MERGE INTO SYS_UPDATE_DATA KEY(ID) VALUES (#{id}, #{msg}, #{module}, #{opt}, 0)
  </insert>
  
  <update id="update" parameterType="java.util.List">
   UPDATE SYS_UPDATE_DATA SET STATE = 1
    WHERE ID IN
    <foreach collection="list" item="item" index="index" separator=","  open="(" close=")">  
    #{item.id}
   </foreach> 
  </update>
  
</mapper>